{
  "packages": ["packages/*"],
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "allowBranch": "develop",
      "message": "chore(release): publish %s",
      "conventionalCommits": true
    }
  },
  "ignoreChanges": ["**/*.spec.*", "**/*.md"]
}
